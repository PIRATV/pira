<div class="highlightSection">
  <div class="container">
    <div class="row">
      <%= render 'contact_info' %>
      <div class="col-md-4 align-right">
        <%= link_to t('Contact'), create_url(t 'Contact'), class: 'btn btn-brand' %>
        </p>
      </div>
    </div>
  </div>
</div>

<div class="container services-main">
  <div class="row">
    <%

       document = Hash.from_xml File.read("#{theme_path}views/xml/services.#{locale}.xml")
       i = 0
       document['services']['service'].each do |info| %>
        <%= render partial: '/site/service',
                   locals: {
                           image: info['image'], url: info['url'], title: info['title'], lists: info['lists'], price: info['price']
                   }
        %>
        <% if (i += 1) == 3 %>
            <% i = 0 %>
            </div>
            </div>
            <div class="container services-main">
              <div class="row">
        <% end %>
    <% end %>
  </div>
</div>
<br>